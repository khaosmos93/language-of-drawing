__pycache__/
*.pyc
*.pyo
.venv/
.env
out/
*.egg-info/
.pytest_cache/
.DS_Store

# local-dev only: symlink so `python -m http.server --directory web` can
# serve core/*.py alongside the web UI. The Pages workflow copies core/
# into the deployed artifact, so this file is not needed in the repo.
web/core
